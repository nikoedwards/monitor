fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
Pillow>=10.0
# Optional but recommended for reliable full-page screenshots:
#   pip install playwright && python -m playwright install chromium
# Without Playwright the snapshot engine falls back to an installed
# Edge/Chrome browser, then to an SVG text snapshot.
